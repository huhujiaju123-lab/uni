# 瑞幸美国（Lucky US）用户分层分析报告

> **Lucky US 数据分析团队**
> 报告日期：2026年2月10日
> 数据范围：2025年11月 - 2026年2月

---

## 目录

1. [方法论框架](#一方法论框架)
2. [RFM用户分层分析](#二rfm用户分层分析)
3. [用户生命周期分析](#三用户生命周期分析)
4. [消费行为分析](#四消费行为分析)
5. [综合运营策略矩阵](#五综合运营策略矩阵)
6. [行动计划与监控](#六行动计划与监控)

---

## 一、方法论框架

### 1.1 分层体系总览

本报告采用**四维分层体系**，多角度刻画用户画像：

| 维度 | 模型 | 核心问题 | 产出 |
|------|------|---------|------|
| **价值维度** | RFM模型 | 用户值多少钱？ | 8大价值群体 |
| **时间维度** | 生命周期模型 | 用户处于哪个阶段？ | 5大生命周期 |
| **行为维度** | 消费行为分析 | 用户怎么消费？ | 频次/客单/品类/时段 |
| **综合维度** | 价值金字塔 | 资源怎么分配？ | 头部/中部/底部 |

### 1.2 数据基础

- **数据库**：Doris（CyberData平台）
- **租户**：`tenant = 'LKUS'`
- **门店**：30+ 家（纽约/新泽西地区），排除测试店
- **统计口径**：成功订单（`status = 90`），美东时区

### 1.3 核心数据表

| 分析场景 | 推荐表 | 查询速度 |
|---------|--------|---------|
| 门店杯量/销售 | `dw_ads.ads_mg_sku_shop_sales_statistic_d_1d` | 秒出 |
| 周复购率 | `dw_ads.ads_user_order_rep_info_d_nw` | 秒出 |
| 月复购率 | `dw_ads.ads_user_order_rep_info_d_nm` | 秒出 |
| 门店维度 | `dw_dim.dim_shop_d_his` | 秒出 |
| 订单明细 | `v_order` + `t_order_item` | 较慢，谨慎使用 |

---

## 二、RFM用户分层分析

### 2.1 RFM评分标准（1-5分制）

#### R维度：最近消费距今天数

| 分数 | 距今天数 | 用户状态 |
|------|---------|---------|
| 5分 | 1-7天 | 超活跃 |
| 4分 | 8-14天 | 活跃 |
| 3分 | 15-30天 | 一般 |
| 2分 | 31-60天 | 沉睡 |
| 1分 | 60天+ | 流失 |

#### F维度：90天消费频次

| 分数 | 消费次数 | 用户状态 |
|------|---------|---------|
| 5分 | 10次+ | 超高频 |
| 4分 | 7-9次 | 高频 |
| 3分 | 4-6次 | 中频 |
| 2分 | 2-3次 | 低频 |
| 1分 | 1次 | 单次 |

#### M维度：90天消费金额

| 分数 | 消费金额 | 用户状态 |
|------|---------|---------|
| 5分 | $100+ | 高价值 |
| 4分 | $60-99 | 较高价值 |
| 3分 | $30-59 | 中等 |
| 2分 | $15-29 | 低价值 |
| 1分 | <$15 | 微价值 |

### 2.2 八大用户群体

| 群体 | RFM特征 | 估算占比 | 优先级 | 核心策略 |
|------|---------|---------|--------|---------|
| **重要价值客户** | R=5, F≥4, M≥4 | 5-8% | P0 | VIP权益、专属折扣、新品试饮 |
| **重要发展客户** | R≥4, F=2-3, M≥4 | 8-12% | P1 | 周卡推广、时段促销、培养频次 |
| **重要保持客户** | R≥4, F≥4, M=2-3 | 10-15% | P1 | 套餐组合、升杯引导、提升客单 |
| **重要挽留客户** | R≤2, F≥4, M≥4 | 5-8% | P0 | 专属召回券、多渠道触达 |
| **一般价值客户** | R≥3, F=2-3, M=2-3 | 20-25% | P2 | 常规促销、积分兑换 |
| **一般发展客户** | R≥4, F≤2, M≤2 | 15-20% | P2 | 新客券包、爆款引导 |
| **一般保持客户** | R=2-3, F=2-3 | 15-20% | P3 | 唤醒提醒、限时秒杀 |
| **一般挽留客户** | R=1, F=1, M=1 | 15-25% | P3 | 低成本触达、止损放弃 |

### 2.3 RFM分层SQL

```sql
WITH user_rfm AS (
    SELECT
        user_no,
        CASE
            WHEN DATEDIFF(CURRENT_DATE, MAX(DATE(CONVERT_TZ(create_time, @@time_zone, 'America/New_York')))) BETWEEN 1 AND 7 THEN 5
            WHEN DATEDIFF(CURRENT_DATE, MAX(DATE(CONVERT_TZ(create_time, @@time_zone, 'America/New_York')))) BETWEEN 8 AND 14 THEN 4
            WHEN DATEDIFF(CURRENT_DATE, MAX(DATE(CONVERT_TZ(create_time, @@time_zone, 'America/New_York')))) BETWEEN 15 AND 30 THEN 3
            WHEN DATEDIFF(CURRENT_DATE, MAX(DATE(CONVERT_TZ(create_time, @@time_zone, 'America/New_York')))) BETWEEN 31 AND 60 THEN 2
            ELSE 1
        END AS R_score,
        CASE
            WHEN COUNT(DISTINCT id) >= 10 THEN 5
            WHEN COUNT(DISTINCT id) BETWEEN 7 AND 9 THEN 4
            WHEN COUNT(DISTINCT id) BETWEEN 4 AND 6 THEN 3
            WHEN COUNT(DISTINCT id) BETWEEN 2 AND 3 THEN 2
            ELSE 1
        END AS F_score,
        CASE
            WHEN SUM(pay_money) >= 100 THEN 5
            WHEN SUM(pay_money) BETWEEN 60 AND 99 THEN 4
            WHEN SUM(pay_money) BETWEEN 30 AND 59 THEN 3
            WHEN SUM(pay_money) BETWEEN 15 AND 29 THEN 2
            ELSE 1
        END AS M_score,
        MAX(DATE(CONVERT_TZ(create_time, @@time_zone, 'America/New_York'))) AS last_order_date,
        COUNT(DISTINCT id) AS order_count_90d,
        ROUND(SUM(pay_money), 2) AS total_amount_90d
    FROM ods_luckyus_sales_order.v_order
    WHERE status = 90 AND tenant = 'LKUS'
      AND shop_name NOT IN ('NJ Test Kitchen', 'NJ Test Kitchen 2')
      AND DATE(CONVERT_TZ(create_time, @@time_zone, 'America/New_York')) >= DATE_SUB(CURRENT_DATE, INTERVAL 90 DAY)
    GROUP BY user_no
)
SELECT
    CASE
        WHEN R_score + F_score + M_score >= 13 AND F_score >= 4 AND M_score >= 4 THEN '重要价值客户'
        WHEN R_score + F_score + M_score >= 11 AND R_score >= 4 AND F_score <= 3 THEN '重要发展客户'
        WHEN R_score + F_score + M_score >= 11 AND R_score >= 4 AND F_score >= 4 AND M_score <= 3 THEN '重要保持客户'
        WHEN R_score + F_score + M_score >= 9 AND R_score <= 2 AND F_score >= 4 AND M_score >= 4 THEN '重要挽留客户'
        WHEN R_score + F_score + M_score BETWEEN 7 AND 10 AND R_score >= 3 THEN '一般价值客户'
        WHEN R_score + F_score + M_score BETWEEN 6 AND 8 AND R_score >= 4 AND F_score <= 2 THEN '一般发展客户'
        WHEN R_score + F_score + M_score BETWEEN 5 AND 7 AND R_score BETWEEN 2 AND 3 THEN '一般保持客户'
        ELSE '一般挽留客户'
    END AS user_segment,
    COUNT(DISTINCT user_no) AS user_count,
    ROUND(AVG(order_count_90d), 2) AS avg_orders,
    ROUND(AVG(total_amount_90d), 2) AS avg_revenue,
    ROUND(SUM(total_amount_90d), 2) AS total_revenue
FROM user_rfm
GROUP BY user_segment
ORDER BY total_revenue DESC;
```

---

## 三、用户生命周期分析

### 3.1 五大生命周期阶段

```
用户旅程：新客期 → 成长期 → 成熟期
                ↓        ↓        ↓
           沉睡期 ← ← ← ← ← ← 流失期
```

| 阶段 | 定义 | 核心指标 | 运营目标 |
|------|------|---------|---------|
| **新客期（0-30天）** | 首单后30天内 | 7日留存率 >20%、二单转化 <12天 | 首单→复购转化 |
| **成长期（31-90天）** | 有复购行为 | 月均频次 >4次、品类渗透 >45% | 培养消费习惯 |
| **成熟期（90天+）** | 稳定消费 | 月频次 >8次、LTV >$200 | 维护忠诚度 |
| **沉睡期（30-60天未消费）** | 活跃度下降 | 唤醒率 >20%、唤醒后留存 >45% | 预警干预 |
| **流失期（60天+未消费）** | 长期未消费 | 召回率 >10%、召回ROI >1:4 | 低成本召回 |

### 3.2 转化漏斗

```
100% 新客注册
  ↓ (首单转化率 60-70%)
65% 完成首单
  ↓ (7日留存 20-25%)
15% 7天内复购
  ↓ (30日留存 35-45%)
25% 进入成长期
  ↓ (60日留存 20-30%)
17% 进入成熟期
  ↓ (90日留存 15-25%)
12% 成为忠诚用户
```

**关键流失节点**：
- 首单后未复购（55-65%流失）→ 48小时黄金窗口，连续3天发券
- 7-14天流失（40-50%）→ 时段促销 + Push提醒
- 30-60天沉睡（20-30%）→ 唤醒券 + 新品试饮

### 3.3 Cohort留存SQL

```sql
WITH user_first_order AS (
    SELECT user_no,
        DATE_FORMAT(MIN(DATE(CONVERT_TZ(create_time, @@time_zone, 'America/New_York'))), '%Y-%m') AS cohort_month,
        MIN(DATE(CONVERT_TZ(create_time, @@time_zone, 'America/New_York'))) AS first_order_date
    FROM ods_luckyus_sales_order.v_order
    WHERE status = 90 AND tenant = 'LKUS'
      AND shop_name NOT IN ('NJ Test Kitchen', 'NJ Test Kitchen 2')
    GROUP BY user_no
),
user_orders AS (
    SELECT user_no, DATE(CONVERT_TZ(create_time, @@time_zone, 'America/New_York')) AS order_date
    FROM ods_luckyus_sales_order.v_order
    WHERE status = 90 AND tenant = 'LKUS'
      AND shop_name NOT IN ('NJ Test Kitchen', 'NJ Test Kitchen 2')
)
SELECT
    f.cohort_month AS 首单月份,
    COUNT(DISTINCT f.user_no) AS 新客数,
    ROUND(COUNT(DISTINCT CASE WHEN o.order_date BETWEEN f.first_order_date AND DATE_ADD(f.first_order_date, INTERVAL 7 DAY) AND o.order_date > f.first_order_date THEN f.user_no END) * 100.0 / COUNT(DISTINCT f.user_no), 2) AS '7日留存%',
    ROUND(COUNT(DISTINCT CASE WHEN o.order_date BETWEEN f.first_order_date AND DATE_ADD(f.first_order_date, INTERVAL 30 DAY) AND o.order_date > f.first_order_date THEN f.user_no END) * 100.0 / COUNT(DISTINCT f.user_no), 2) AS '30日留存%',
    ROUND(COUNT(DISTINCT CASE WHEN o.order_date BETWEEN f.first_order_date AND DATE_ADD(f.first_order_date, INTERVAL 90 DAY) AND o.order_date > f.first_order_date THEN f.user_no END) * 100.0 / COUNT(DISTINCT f.user_no), 2) AS '90日留存%'
FROM user_first_order f
LEFT JOIN user_orders o ON f.user_no = o.user_no
WHERE f.cohort_month >= '2025-08'
GROUP BY f.cohort_month
ORDER BY f.cohort_month DESC;
```

---

## 四、消费行为分析

### 4.1 消费频次分布

| 频次层级 | 定义 | 估算占比 | 特征 | 策略 |
|---------|------|---------|------|------|
| 单次用户 | 仅1单/月 | 40-50% | 新客流失高危 | 48h复购券 |
| 低频用户 | 2-3单/月 | 25-30% | 习惯未养成 | 周度满减 |
| 中频用户 | 4-8单/月 | 15-20% | 稳定贡献层 | 价值深挖 |
| 高频用户 | 9+单/月 | 5-10% | 核心忠诚 | VIP维护 |

### 4.2 客单价分层

| 层级 | 定义 | 用户画像 | 策略 |
|------|------|---------|------|
| 低客单 | <$5 | 价格敏感，基础款 | 阶梯满减 |
| 中客单 | $5-$8 | 主流群体，复购最稳 | 保持+引导升杯 |
| 高客单 | $8+ | 品质追求型 | VIP专属权益 |

### 4.3 品类偏好

| 品类 | 典型产品 | 运营策略 |
|------|---------|---------|
| 经典咖啡 | 美式、拿铁 | 订阅制/会员卡锁定价值 |
| 特调饮品 | 生椰拿铁、厚乳拿铁 | 新品首发推送、社交分享 |
| 非咖啡 | 茶饮、果汁 | 品类交叉销售 |

### 4.4 时段消费

| 时段 | 时间 | 消费动机 | 策略 |
|------|------|---------|------|
| 早高峰 | 7-10am | 通勤提神 | 提前下单、到店即取 |
| 午间 | 11am-2pm | 午后续航 | 午餐套餐组合 |
| 下午茶 | 2-5pm | 休闲放松 | 限时折扣、社交分享 |
| 晚间 | 5pm+ | 加班续杯 | 低因推荐、夜间卡 |

### 4.5 门店销售对比SQL（秒出）

```sql
SELECT
    shop_name AS 门店,
    SUM(sku_cnt) AS 总杯量,
    ROUND(SUM(sku_cnt) / COUNT(DISTINCT dt), 0) AS 日均杯量,
    ROUND(SUM(pay_amount), 2) AS 总销售额,
    ROUND(SUM(pay_amount) / SUM(sku_cnt), 2) AS 杯均价
FROM dw_ads.ads_mg_sku_shop_sales_statistic_d_1d
WHERE dt BETWEEN '2026-01-01' AND '2026-01-31'
  AND tenant = 'LKUS'
  AND one_category_name = 'Drink'
  AND shop_name NOT IN ('NJ Test Kitchen', 'NJ Test Kitchen 2')
GROUP BY shop_name
ORDER BY 日均杯量 DESC;
```

---

## 五、综合运营策略矩阵

### 5.1 用户分层 x 运营动作

| 用户类型 | 优惠券 | 推送 | 会员权益 | 品类引导 |
|---------|--------|------|---------|---------|
| **高价值（VIP）** | 专属折扣券 | 每周≤3次 | 钻石卡（2倍积分、免费升杯） | 新品试饮权 |
| **成长期** | 频次券（满4送1） | 每周≤2次 | 金卡（1.5倍积分） | 交叉品类推荐 |
| **沉睡用户** | 高力度召回券（$5无门槛） | 每月1次 | 保级提醒 | 推送偏好品类新品 |
| **价格敏感** | 阶梯满减（$10减$3） | 促销日推送 | 积分兑换 | 高性价比款推荐 |
| **新客** | 连续3天券包 | 48h黄金窗口 | 注册即享积分 | 爆款TOP5引导 |

### 5.2 会员体系（四级）

| 等级 | 门槛 | 核心权益 |
|------|------|---------|
| 普通会员 | 注册即享 | 生日券、基础积分 |
| 银卡 | 月消费$50+ | 1.2倍积分、升杯1次/月 |
| 金卡 | 月消费$100+ | 1.5倍积分、买一送一券/月、新品试饮 |
| 钻石 | 月消费$200+ | 2倍积分、买一送一2次/月、免费升杯不限次 |

### 5.3 精准推送时机表

| 触发条件 | 推送时间 | 内容 | 渠道 |
|---------|---------|------|------|
| 新客48h未复购 | 首购后48h | "第2杯7折等你来" | Push + SMS |
| 高频用户3天未消费 | 距上次72h | "双倍积分日到了" | Push |
| 低频用户周末前 | 每周五15:00 | "周末下午茶5折起" | Push |
| 沉睡30天 | 距上次30天 | "好久不见！惊喜券包已到" | Push + Email |
| 新品上市 | 首日10:00 | "新品首发，前100杯买一送一" | Push |

---

## 六、行动计划与监控

### 6.1 实施路线图

| 阶段 | 时间 | 行动 |
|------|------|------|
| **第1-2周** | 立即 | 搭建RFM自动分层 + 新客48h推送 + 沉睡召回SOP |
| **第3-4周** | 本月 | 叠加生命周期 + VIP权益上线 + 下午茶时段营销 |
| **第5-8周** | 下月 | 品类交叉销售 + 区域化试点 + AB实验验证 |
| **第9-12周** | 季度 | 会员体系升级 + 自动化标签 + 个性化推荐 |

### 6.2 核心监控指标

```sql
-- 每日核心指标（秒出）
SELECT dt AS 日期,
    SUM(sku_cnt) AS 总杯量,
    SUM(order_cnt) AS 总订单,
    ROUND(SUM(pay_amount), 2) AS 总销售额,
    ROUND(SUM(pay_amount) / SUM(order_cnt), 2) AS 客单价
FROM dw_ads.ads_mg_sku_shop_sales_statistic_d_1d
WHERE dt >= DATE_SUB(CURDATE(), INTERVAL 7 DAY)
  AND tenant = 'LKUS' AND one_category_name = 'Drink'
  AND shop_name NOT IN ('NJ Test Kitchen', 'NJ Test Kitchen 2')
GROUP BY dt ORDER BY dt DESC;
```

```sql
-- 周复购率趋势（秒出）
SELECT src_week_first_date AS 起始周,
    src_usr_cnt AS 起始用户数,
    dst_usr_cnt AS 复购用户数,
    ROUND(dst_usr_cnt * 100.0 / src_usr_cnt, 2) AS 复购率
FROM dw_ads.ads_user_order_rep_info_d_nw
WHERE src_week_first_date >= DATE_SUB(CURDATE(), INTERVAL 8 WEEK)
  AND DATEDIFF(dst_week_first_date, src_week_first_date) = 7
ORDER BY src_week_first_date DESC;
```

### 6.3 效果评估标准

| 指标 | 基线 | 目标 | 优秀 |
|------|------|------|------|
| 7日留存率 | 20% | 25% | 30% |
| 30日留存率 | 35% | 42% | 50% |
| 月复购率 | 30% | 40% | 50% |
| 召回率（沉睡） | 15% | 20% | 30% |
| 营销ROI | 1:3 | 1:4 | 1:5 |

---

**报告附件**：
- `用户分层报告_方法论.md` — 完整方法论框架（含价值金字塔、咖啡行业特有维度）
- `用户分层报告_RFM分析.md` — RFM详细分析（含8群体策略、SQL模板）
- `用户分层报告_生命周期.md` — 生命周期详细分析（含Cohort留存SQL）
- `用户分层报告_消费行为与策略.md` — 消费行为分析（含运营日历、券包设计）

---

*本报告由 Agent Swarm 协作生成（3个Agent并行编写），基于 Lucky US Doris 数据库结构设计*
*所有SQL模板可直接在 CyberData 平台执行*
