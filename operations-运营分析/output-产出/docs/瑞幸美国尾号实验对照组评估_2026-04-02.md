# 瑞幸美国尾号实验对照组评估

## 结论

尾号 `0` 最接近瑞幸美国大盘，适合作为优先对照组。

备选尾号是 `4`。`9` 的综合偏差分虽然也不高，但杯量质量代理偏差较大，不建议优先作为对照组。

## 核心假设

- 数据源：CyberData
- 用户池：`2026-04-01` 的 `LKUS` 全量用户
- 尾号口径：`dw_ads.user_label_df.ab_bash_10000` 的个位尾号 `0-9`
- 时间窗：近 `30` 天
- 使用场景：内部决策口径

## 口径说明

- 本次已经跑通 `dw_ads.user_label_df` 全量尾号分布和消费代理指标。
- 近 `30` 天 `v_order` 的精确杯量和实收聚合，在当前 CyberData 队列里没有成功返回。
- 因此这次“均匀性”判断采用的是代理指标，不是最终精确杯量/实收口径。
- 当前代理指标包括：
- 用户占比
- 近30天新客占比
- 近30天下单率
- 近30天人均订单数
- 180天人均杯量代理
- 消费金额代理

## 数据总览

大盘基线：

- 用户占比：`10.00%`
- 近30天新客占比：`11.41%`
- 近30天下单率：`0.282%`
- 近30天人均订单数：`0.00342`
- 180天人均杯量代理：`0.00243`
- 消费金额代理：`3.0135`

各尾号表现：

- 尾号 `0`：用户占比 `10.10%`，近30天新客占比 `11.26%`，近30天下单率 `0.269%`，近30天人均订单 `0.00396`，杯量代理 `0.00239`，实收代理 `2.9267`，综合偏差分 `0.274`
- 尾号 `4`：用户占比 `10.04%`，近30天新客占比 `11.36%`，近30天下单率 `0.322%`，近30天人均订单 `0.00368`，杯量代理 `0.00261`，实收代理 `3.0540`，综合偏差分 `0.314`
- 尾号 `3`：用户占比 `9.95%`，近30天新客占比 `11.05%`，近30天下单率 `0.284%`，近30天人均订单 `0.00371`，杯量代理 `0.00284`，实收代理 `3.1271`，综合偏差分 `0.336`
- 尾号 `7`：用户占比 `9.92%`，近30天新客占比 `11.87%`，近30天下单率 `0.341%`，近30天人均订单 `0.00367`，杯量代理 `0.00243`，实收代理 `2.9400`，综合偏差分 `0.358`
- 尾号 `2`：用户占比 `9.95%`，近30天新客占比 `11.35%`，近30天下单率 `0.232%`，近30天人均订单 `0.00273`，杯量代理 `0.00242`，实收代理 `2.9687`，综合偏差分 `0.405`
- 尾号 `6`：用户占比 `9.98%`，近30天新客占比 `11.37%`，近30天下单率 `0.334%`，近30天人均订单 `0.00391`，杯量代理 `0.00216`，实收代理 `3.1187`，综合偏差分 `0.478`
- 尾号 `8`：用户占比 `10.02%`，近30天新客占比 `11.67%`，近30天下单率 `0.338%`，近30天人均订单 `0.00425`，杯量代理 `0.00246`，实收代理 `3.0559`，综合偏差分 `0.492`
- 尾号 `5`：用户占比 `10.11%`，近30天新客占比 `11.24%`，近30天下单率 `0.223%`，近30天人均订单 `0.00254`，杯量代理 `0.00249`，实收代理 `2.9148`，综合偏差分 `0.549`
- 尾号 `1`：用户占比 `9.81%`，近30天新客占比 `10.96%`，近30天下单率 `0.194%`，近30天人均订单 `0.00235`，杯量代理 `0.00256`，实收代理 `3.0115`，综合偏差分 `0.741`
- 尾号 `9`：用户占比 `10.12%`，近30天新客占比 `12.01%`，近30天下单率 `0.284%`，近30天人均订单 `0.00335`，杯量代理 `0.00193`，实收代理 `3.0190`，综合偏差分 `0.299`

## 分析与解读

- 尾号 `0` 的综合偏差分最低，是当前最像大盘的一组。
- 尾号 `0` 的优势在于多个核心维度都贴近大盘，没有明显单点跑偏。
- 尾号 `4` 不是总分最低，但各项波动都比较平，适合作为更稳妥的备选组。
- 尾号 `9` 虽然总分靠前，但 `180` 天杯量代理相对大盘偏低约 `20%`，质量不够稳。
- 尾号 `1`、`5`、`8` 在下单率或人均订单数上偏离更明显，不适合做对照组。
- 这次结论可以支持“哪个尾号更像大盘”的判断，但不能替代近 `30` 天精确 `杯量/实收` 结果。

## 建议

- 后续尾号实验默认使用尾号 `0` 作为对照组。
- 需要备用组时，优先考虑尾号 `4`。
- 当前不建议使用尾号 `9` 作为第一优先对照组。
- 等 CyberData 队列恢复后，再补跑一次近 `30` 天精确 `杯量/实收` 校准最终方案。
