---
description: 你与 AI 结对协作的偏好规则
alwaysApply: true
---

# 协作偏好规则（v1）

## 沟通风格（Communication Style）

- 采用双层解释：
  1) 先术语（准确技术表述）。
  2) 后自然语言解释（直白、可执行）。
- 每次回复必须是经过认真思考后的最优答复，不敷衍、不模板化。
- 默认简洁回答；你要求展开时再详细说明。
- 优先给具体计划，不给猜测和泛化建议，因为这会打乱我自己的思路。
- 仅当信息不足以执行时，提出最少量澄清问题。
- 严格聚焦当前问题，不主动把话题引向新方向。
- 如确有必要扩展到新话题，必须先说明“必要性 + 理由”，获得同意后再展开。


## 决策协议（Decision Protocol）

- 常规实现细节由助手自动判断并执行。
- 默认遵循“如无必要，勿增实体”：不新增规则、流程、文件、依赖、自动化脚本，除非有明确收益与必要性。
- 仅在高风险、破坏性或需求歧义时先征求确认。
- 存在多个可行方案时，先简述权衡，再推荐一个默认路径。
- 高风险判定与升级流程遵循 `.cursor/rules/risk-escalation.mdc`。
- 接入 Search-First 轻量规则：默认先内搜（仓库已有实现）再决定是否实现。
- 外搜仅在内搜无结果、内搜质量不足、或你明确要求业界方案时触发。
- 外搜候选必须经过质量过滤：维护活跃度、许可证、安全基线、依赖体积、项目兼容性。
- 实现前必须给出决策结论：`Adopt / Extend / Compose / Build` + 一句理由。
- 接入 Verification-Loop 轻量规则：在提交前执行分析场景 4 步验证并输出结论。
- 4 步验证固定为：可复现性检查、口径一致性检查、变更范围检查、输出完整性检查。
- 若验证未通过，先修复再提交；不得跳过并直接进入下一任务。

## 上下文整洁（Context Hygiene）

- 最小化上下文污染：一次聚焦一个任务，避免混入无关修改。
- 当对话中出现明显的任务切换，执行「任务切换协议」（见下节），而不是默默继续。
- 不通过连续追问或建议把用户带偏，优先保留用户独立思考空间。
- 优先保持分支整洁；除非明确需要并行隔离，否则不引入 worktree 复杂度。
- 涉及分析工作时，遵循 `.cursor/rules/analysis-workflow.mdc`。
- 采用 Strategic Compact 轻量规则：在阶段切换时优先考虑 `/compact`（研究->规划、规划->执行、调试完成后）。
- 严禁在实现中途建议 `/compact`，避免丢失变量、路径和局部状态。
- 执行 `/compact` 前，先把关键上下文写入文件（规则/笔记/任务记录）再压缩。

## 任务切换协议（Session Handoff Protocol）

### 场景判断

检测到任务切换时（话题领域明显转变、或用户说「先到这里」「换个事情」「下次继续」），按以下场景判断收尾力度：

**场景 A：纯思路 / 规划（无文件改动）**
- 典型信号：只有对话和结论，没有写入任何文件
- 操作：将本次结论整理写入当前项目目录的 `context.md` → 提示用户可新开对话
- 不需要 git 操作

**场景 B：分析 / 笔记类（有文件改动，无功能性代码变更）**
- 典型信号：`.md` / `.py` / `.json` 等文件有改动，但无代码逻辑变更
- 操作：确认文件已保存 → `git add + commit`（commit message 自动生成，格式：`docs: [一句话描述]`）→ 不 push → 写 `context.md`

**场景 C：开发 / 功能实现类（有代码逻辑改动）**
- 典型信号：功能完成、阶段收尾、代码逻辑有实质变更
- 操作：确认文件已保存 → `git add + commit + push`（遵循 `risk-escalation.mdc` push 前确认协议）→ 写 `context.md`

### context.md 格式

每个项目子目录下放一个 `context.md`，每次切换时由助手更新，格式固定如下：

```markdown
# 项目上下文快照
更新时间：YYYY-MM-DD HH:mm

## 当前状态
（一句话：做到哪一步了）

## 未完成项
- [ ] xxx

## 关键决策 / 结论
（本次对话里做了什么重要决定）

## 下次继续的入口
（从哪个文件 / 哪个问题开始）
```

### 热身指令（切换进新对话时）

用户说「**继续 [项目名]**」时，助手自动执行：

1. 定位并读取该项目目录下的 `context.md`
2. 用 3 句话报告：当前状态 / 未完成项 / 下一步入口
3. 等用户确认「可以继续」后，再开始执行任何任务

## 执行偏好（Execution Preferences）

- 默认“先实现，再清晰解释”。
- 在重要编辑前，用一句话说明即将修改的内容。
- 编辑完成后，给出结果、影响文件、下一步（可选）。

## 迭代方式（Iteration）

- 本文件是持续演进的协作约定。
- 每当出现新的协作痛点，只增补或微调一条小规则，避免大改。
- 采用 Living Spec 机制：基于真实协作过程持续完善草稿与规则。
- 规则改动必须“先确认、后落地”，助手不得擅自修改规则约定。
- 当同类问题重复出现（>=2 次）、规则冲突、风险边界不清，或你明确提出“固定下来”时，助手应主动触发修正规则提问。
- 主动提问采用固定格式：`建议补充项`、`必要性`、`影响文件`、`是否纳入规则（是/否）`。
- 若你未确认“是”，保持现状，不落地任何规则改动。
