# Vibe Coding 全局规范（所有模型通用）

适用范围：`/Users/xiaoxiao/Vibe coding` 下全部项目。
适用对象：Codex、Claude Code、以及其他模型的 coding 工作流。
优先级：本文件为全局顶层规则，高于子项目内局部约定。

## 1. Markdown 链接强制规则
1. 项目内文件一律使用相对路径链接（如 `./foo.md`、`../bar.md`）。
2. 禁止把目录当文件链接；目录必须先有索引文件再链接。
3. 核心文档中不放长绝对路径；外部资源统一通过“参考资料入口页”中转。
4. 写完链接后必须手动点击验证一次再提交。
5. 本地系统文件若需引用，优先在项目内建立中转入口，不在业务文档中散落。

## 1.1 工作区会话启动协议
1. 任何在 `/Users/xiaoxiao/Vibe coding` 内启动的 Codex、Claude Code 或其他模型会话，进入任务前必须先读取：
   - 根目录 `AGENTS.md`
   - 根目录 `VIBECODING_GLOBAL_RULES.md`
   - 根目录 `README.md`
2. 当任务落在某个子目录时，必须继续读取该目录及最近上级目录中的相关 Markdown 文档，再开始执行。
3. 根目录规则属于高优先级约束；子目录 Markdown 负责补充任务上下文，不得覆盖根目录规则。
4. 未找到相关目录 Markdown 时，必须明确说明“未找到相关目录文档”，不得默认跳过。
5. 本协议适用于同一工作区下的所有窗口、所有新会话，作为启动阶段的强制动作。

## 1.2 命名与路径展示规则（强制）
1. 文件名、目录名、文档标题、对用户展示的路径中，禁止出现 URL 编码串（如 `%E8%BF%90%E8%90%A5`）或其他乱码形式；展示路径时必须使用原始可读名称，不得把编码后的链接文本当作文件名展示。
2. 长期保留的项目目录、业务目录、模块目录统一使用 `english-中文` 格式：英文在前，中文在后，中间仅用一个连字符连接。
3. Markdown 文档优先使用中文直述命名；需要排序时使用 `序号_中文主题.md`，例如 `00_项目总览.md`、`04_协作使用说明.md`。
4. 脚本、代码文件、自动化入口统一使用纯英文、小写、可读命名，优先 `kebab-case` 或 `snake_case`；中文语义优先放在上级目录中体现，不直接写进脚本文件名。
5. 目录入口文件统一优先使用 `README.md`；需要索引页时使用 `index.md`，禁止为同一目录并行创建多个语义重复的入口文件。
6. 文档、回复、说明中引用路径时，必须使用“中文说明（英文语义）+ 原始可读路径”的写法；不要直接抛编码后的长链接，不要把系统自动编码结果展示给用户。
7. 新建文件前必须先判断归属目录；禁止在根目录或无关目录长期存放 `Untitled.md`、`temp.md`、`测试.md`、`新建文档.md` 这类临时命名文件。
8. 需要长期保留的正式 Markdown 文档，默认命名为 `english-中文主题.md`；只有目录内已经明确采用编号体系时，才允许使用 `序号_中文主题.md`。
9. 回复用户时，文件引用的可见标签必须是可读中文或中英文对照名称；禁止把绝对路径、URL 编码串、终端转义结果当作展示名称。
10. 新建文件或目录时，必须先以“全局项目管理”视角判断归属：先判断属于哪个一级项目，再判断属于该项目下的哪一类子目录（说明、产出、脚本、资料、日志、配置等），最后再创建；禁止跳过归属判断直接落盘。
11. 文件与目录命名必须围绕“更好迭代、更好检索、更好协作、更好项目管理”展开；命名应尽量体现主题、对象、阶段、时间或用途，避免过短、过泛、难复用的名字。
12. 面向长期维护的正式项目内容，默认采用中英文对照体系并与所在目录保持一致：
   - 目录优先使用 `english-中文`
   - 正式 Markdown 文档优先使用 `english-中文主题.md`
   - 仅当当前目录已明确采用编号体系时，才使用 `序号_中文主题.md`
13. 若现有目录规则与默认规则不同，先遵循该目录已存在的稳定命名体系，但新增内容仍需保持“可迭代、可管理、可检索”的命名质量，不得破坏上下文一致性。

## 1.3 文档存放规则（命名配套）
1. 根目录只放全局规则、总入口、跨项目配置，不放具体业务过程文档。
2. 项目说明、执行说明、任务日志、分析结论必须放在对应项目目录，不得散落到其他项目或根目录。
3. 同一主题文档必须集中在一个目录内维护，优先通过 `README.md`、`index.md` 或编号文档组织，不重复创建平行副本。
4. 临时记录若会长期保留，必须在落盘前改成正式命名并移动到正确目录；禁止让临时命名文件长期存在。
5. 新建目录时先明确其用途是“项目目录”“资料目录”“产出目录”还是“脚本目录”，目录名必须直接反映该用途，避免语义含混。
6. 无法立刻判断归属目录时，必须先做归类判断再创建；不能因为信息零散就默认落到根目录、`tmp/` 或其他临时位置。
7. 需要跨项目复用的内容，优先放到对应的公共目录（如 `libs-公共库/`、`knowledge-知识库/`、`tools-工具/`），不要在单一项目目录内伪装成公共资产。

## 2. 文档结构规则
1. 顶层文档只放入口与规则，不堆细节。
2. 子项目文档只写执行与结果，不重复顶层规范。
3. 规则变更先改顶层，再改模板，再改子项目。

## 2.1 知识库与资料库规则
1. `knowledge-知识库/` 用于承接 Obsidian、Calibre 导出、SQL 资料、阅读与参考归档，不放可执行项目源码。
2. `imports-外部导入-staging/` 只做外部目录导入承接与私有配置隔离，不作为长期正式项目目录。
3. 桌面资料、学习资料、转录、参考图文先进入知识或 staging，再决定是否并入业务目录。
4. 应用状态文件（如 `.obsidian/`、`metadata.db`、`notes.db`、`project.private.config.json`）默认不纳入主仓库。

## 2.2 外部项目并入规则
1. 外部项目并入 `Vibe coding` 时，必须移除嵌套 `.git`，统一由主仓库管理。
2. 运行时目录（如 `logs/`、`workspace/`、`data/`、`node_modules/`）优先清理或忽略，不直接并入。
3. 发现同源项目时先去重再迁移，避免同一项目在不同目录下并存。

## 3. 口径冲突规则
1. 事实冲突先确认，不强行落结论。
2. 表/字段冲突以权威来源为准。
3. 未确认项允许保留，但必须显式标注“待确认”。

## 4. 提交前检查
- [ ] 链接可点击
- [ ] 相对路径优先
- [ ] 没有目录直链
- [ ] 顶层与子项目职责清晰

## 5. 提示词优化前缀协议
1. 当用户输入的第一句以以下前缀开头时，助手必须先执行“提示词优化”：
   - `优化提示词：`
   - `优化提示词后执行：`
   - `优化提示词并执行此任务：`
2. `优化提示词：`
   - 仅输出优化结果，不执行任务。
   - 返回内容至少包含：
     - 结构化任务单
     - 优化后的提示词
3. `优化提示词后执行：` 或 `优化提示词并执行此任务：`
   - 先输出优化后的提示词
   - 再基于该优化版本直接执行任务
   - 不要求用户把优化后的提示词再次发回
4. 提示词优化时，默认拆分为以下槽位（按需省略空项）：
   - `task`
   - `materials`
   - `requirements`
   - `output_format`
   - `constraints`
5. 若信息不完整但仍可合理执行，优先补全默认项并继续；只有在存在真正阻塞项时，才提一个关键问题。
6. 为了便于用户学习，凡是触发该协议，助手都必须显式展示“优化后的提示词”。

## 6. 当前权威补充
- 数据分析类表/字段最终以：`/Users/xiaoxiao/Downloads/数据库表说明` 为准。
